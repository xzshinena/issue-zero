# Web
fastapi
uvicorn[standard]

# Config & validation
pydantic
pydantic-settings

# Database (PostgreSQL + pgvector)
psycopg[pool]
psycopg-binary

# GitHub & HTTP
pygithub
httpx
python-dotenv

# GitLab (optional — only needed for GitLab sync)
python-gitlab

# Embeddings (sentence-transformers for local; openai optional for API)
sentence-transformers

# BM25 keyword retrieval
rank_bm25

# ML classifiers
scikit-learn
joblib

# SetFit fine-tuning (urgency classifier)
setfit
datasets

# Optional
# openai
# celery
# redis

# Dev / testing
pytest
pytest-asyncio
pytest-cov
httpx
